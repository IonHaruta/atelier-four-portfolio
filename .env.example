# ============ Frontend (React) ============
# URL of the Django backend API (for contact form)
# Local: http://localhost:8000
# Production: https://your-django-api.example.com
VITE_API_URL=http://localhost:8000

# ============ Backend (Django) - use backend/.env ============
# See backend/README.md for full email configuration
